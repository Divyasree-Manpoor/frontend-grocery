const SharedAccessBadge = () => {
  return (
    <span className="bg-blue-100 text-blue-700 px-4 py-2 rounded-full text-sm font-semibold">
      🔒 Shared Mode
    </span>
  );
};

export default SharedAccessBadge;